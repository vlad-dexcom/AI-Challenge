<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PersonalTrainer">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.PersonalTrainer">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Day 18 debug/QA aid: lets `adb shell am broadcast -a
             com.example.geminichat.RUN_WORKOUT_DIGEST_NOW` force an immediate workout-digest
             run for manual testing (see WorkoutDigestDebugReceiver). Must be exported for adb to
             reach it, but the receiver itself no-ops unless BuildConfig.DEBUG is true. -->
        <receiver
            android:name=".agent.workout.WorkoutDigestDebugReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.example.geminichat.RUN_WORKOUT_DIGEST_NOW" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
