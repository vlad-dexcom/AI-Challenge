# Сценарий проверки планировщика и фоновых задач (Day 18)

Ручной сквозной сценарий для проверки, что "Workout Digest" реально сохраняет данные, выполняется
по расписанию в фоне и отдаёт агрегированный результат.

---

## Подготовка

1. Убедитесь, что в `local.properties` задан рабочий `GEMINI_API_KEY`.
2. Соберите и установите отладочную сборку:
   ```bash
   ./gradlew :app:installDebug
   ```
3. Откройте приложение — попадаете в обычный экран чата.

---

## Шаг 1 — Выбор персоны

В выборе агента/персоны выберите **«Workout Digest (scheduled)»**.

---

## Шаг 2 — Логирование тренировки

Напишите агенту, например:

```
Только что закончил 30-минутную тренировку ног.
```

**Ожидаемый результат:** агент вызывает `log_workout(goal="legs", minutes=30)` и подтверждает, что
тренировка записана (см. блок вызовов инструментов в UI/логах, как в Day 17).

Повторите ещё пару раз с разными `goal`/`minutes`, чтобы накопить данные.

---

## Шаг 3 — Запрос сводки до первого фонового запуска

Сразу после логирования спросите:

```
Как продвигаются мои тренировки на этой неделе?
```

**Ожидаемый результат:** агент вызывает `get_workout_summary()`; так как фоновая задача ещё не
успела отработать, инструмент честно сообщает, что сводка ещё не готова (данные уже записаны, но
агрегация происходит по расписанию, а не мгновенно).

---

## Шаг 4 — Проверка фонового выполнения напрямую (adb)

Расписание — минимум 15 минут (ограничение `WorkManager`), ждать вручную неудобно, поэтому
запустите задачу немедленно через `adb`:

```bash
adb shell cmd jobscheduler run -f com.example.geminichat <job-id>
# или, проще, из Android Studio: App Inspection → Background Task Inspector →
# найдите worker "workout-digest" → Run now.
```

Также можно временно уменьшить интервал в `WorkoutDigestScheduler.schedule(context,
repeatIntervalMinutes = ...)` только для локальной отладки (не коммитить) — минимум, который
реально примет `WorkManager`, всё равно 15 минут.

---

## Шаг 5 — Повторный запрос сводки

Снова спросите про прогресс за неделю.

**Ожидаемый результат:** `get_workout_summary()` теперь возвращает агрегированный JSON — общее
число тренировок, суммарные минуты и разбивку по `goal`, — и агент пересказывает это обычным
языком.

---

## Шаг 6 — Переживает перезапуск процесса

1. Полностью закройте приложение (свайпом из списка задач).
2. Снова откройте его и выберите ту же персону.
3. Спросите сводку ещё раз.

**Ожидаемый результат:** сводка та же (или новее, если фоновая задача успела сработать снова) —
данные и расписание не сбрасываются при перезапуске процесса, поскольку и файлы (`filesDir`), и
регистрация периодической работы в `WorkManager` не зависят от времени жизни `ChatViewModel`.

---

## Автоматические тесты

```bash
./gradlew :app:testDebugUnitTest --tests "com.example.geminichat.agent.workout.*"
./gradlew :app:testDebugUnitTest --tests "com.example.geminichat.mcp.LocalWorkoutMcpGatewayTest"
```
